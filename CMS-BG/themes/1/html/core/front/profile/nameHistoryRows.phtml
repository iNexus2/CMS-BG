<ips:template parameters="$table, $headers, $rows" />
{{if count( $rows )}}
	{{foreach $rows as $row}}
		<li class='ipsDataItem'>
			<div class="ipsDataItem_main ipsType_center">
		   		<h4 class='ipsType_minorHeading'>{datetime="$row['dname_date']"}</h4>
		   		<p class='ipsType_reset ipsType_large'>
		      		{$row['dname_previous']} &nbsp;&nbsp;<i class='fa fa-angle-right'></i>&nbsp;&nbsp; {$row['dname_current']}
		      	</p>
		   </div>
		</li>
	{{endforeach}}
{{endif}}